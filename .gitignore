# Dune / OCaml build output
_build/
*.install
*.merlin

# OCaml compiled artifacts (from bytecode/native builds outside dune)
*.cmi
*.cmo
*.cmx
*.cmt
*.cmti
*.o
*.a
*.byte
*.exe

# opam local switch, if ever created in-tree
_opam/

# Editor / OS
*~
.DS_Store

# Local scratch: the `jack` native binary (a compiled `kan build` output).
# The gate (tests/run_all.sh) still scans examples/*.kan, so keep any scratch
# .kan that must NOT break the gate out of examples/ (e.g. a scratch/ dir).
/examples/jack
